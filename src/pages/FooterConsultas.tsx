const HistorialFooter = () => {
    return (
        <footer className="footer mt-4">
            <p>© {new Date().getFullYear()} - Sistema de Auditoría de Accesos</p>
        </footer>
    );
};

export default HistorialFooter;
